spring:
  application:
    name: currency-rate
  data:
    redis:
      host: localhost
      port: 6379
      password: 88888888

api:
  exchangerate:
    url: http://localhost:9080
    key: test

application:
#  Choose source of exchange rates here. Supported values: exchangerate
  source: exchangerate

  redis:
    ttl: 1m
